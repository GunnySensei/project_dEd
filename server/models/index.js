const User = require('./User');
const DeathFact = require('./DeathFact');
const Donation = require('./Donation');
const Order = require('./Order')

module.exports = { User, DeathFact, Donation, Order };